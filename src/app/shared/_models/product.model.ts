export interface Product {
  id: string;
  name: string;
  description: string;
  price: string;
  imageUrl: string;
  quantity: number;
  thumnail: string;
}


export interface SortOption {
  sort: string;
  viewValue: string;
}
